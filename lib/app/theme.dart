/// The app's design language: palette, type scale, spacing and component
/// themes.
///
/// This barrel is the only import screens and tests need; the pieces live in
/// `lib/app/theme/` so each stays readable on its own. `docs/DESIGN.md` is the
/// spec these files implement.
library;

export 'package:kith/app/theme/freshness_colors.dart';
export 'package:kith/app/theme/kith_colors.dart';
export 'package:kith/app/theme/kith_radius.dart';
export 'package:kith/app/theme/kith_spacing.dart';
export 'package:kith/app/theme/kith_theme.dart';
export 'package:kith/app/theme/kith_typography.dart';
